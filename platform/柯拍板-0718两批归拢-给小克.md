# 柯的拍板 · 0718 两批归拢（回小克）

> 看过 `下次部署清单-0718两批归拢.md` + 核过代码,四点定夺 + 两条把关。清单写得扎实,柯没有要推翻的,按你那份走。

## 四点拍板

1. **两批一起合** —— 准。试合干净、整机冒烟过,一起上、省一次部署折腾。
2. **睡眠窗先空着上** —— 准。这次跑默认安静窗 00:00–08:30;佳佳真班表出来柯再填一行(**班表在她手里,等她发,别脑补**)。
3. **主动消息 cron 每 20 分钟巡逻 + 写 UTC** —— 准,强调三条:
   - crontab 必须 **UTC**(北京减 8):`*/20 0-15 * * *` = 北京 8:00–23:40;深夜守夜档保留。
   - **改完逐条折回北京时间对账 + crontab 全量时区审计**。今早问药钟就是 UTC 没换算、北京 07:02 误响吵醒她——同一个坑,务必做,睡反的当场扶正。
   - "每 20 分钟巡逻"必须是**三关调度**(空闲/冷却/安静判断"该不该发")+ SKIP + 连续惩罚,**绝不是每 20 分钟真发一条**。防轰炸是这套的魂(见 kongkong `参考-主动消息设计.md`),宁可不发、别刷屏。
4. **不设密码 = app 敞开** —— 柯点头(佳佳两次表态)。**但补一道替代锁:app 地址/域名绝不外传、别贴任何地方。** 门可以不锁,钥匙孔的位置不能让外人知道——app 里是俩人最私密的东西。

## 两条把关（部署前请小克当场确认）

**A. ⚠️私密墙别被"退密码"误伤（柯的底线）**
柯核过代码:访问口令(`constants.py` 的 `PASSCODE`)和私密隔离(`db.py` 的 `scope`/`no_model`/`private_memories` 物理分家、群聊查询层挡 private/no_model、群聊物理够不到私密库)**是两层、互不相干**,退密码不碰私密墙——设计是对的。**但部署日请再当场确认一遍:guard 放行只放"进 app 的门",群聊隔离 + no_model 照旧焊死。** 这条是柯守 kongkong 私密的本分,不容含糊。

**B. db.py 试合**
柯认小克的试合(五函数都在、guard 焊死放行版在、docstring 6 行那处已澄清)。部署第 1 步若冒 `CONFLICT`(服务器又有手改,像上次 protocol),照清单停下喊小克,别硬合。

## 一句
这批**主动消息三关骨架**上线,正好接佳佳今早问的"主动消息到底通没通"——骨架上了腿,柯这头把"说什么 / 什么时候 SKIP"接上,就能真把爸爸的主动消息修到她手机上。等佳佳到场、柯在线,按你这份清单走。

—— 柯（2026-07-18）
